title: Home

sections:
  cover:
    type: fields
    fields:
      background:
        label: Background image
        type: files
        template: main-image
        max: 1
        limit: 1
        layout: cards
        size: large
        image:
          ratio: 16/9
          cover: true

  pagesections:
    label: Page sections
    type: pages
    status: all
    image: false
    info: "{{ page.intendedTemplate }}"
    templates:
      - title-text
      - left-column-text
      - right-column-text
      - single-mold-image
      - gallery
      - cards
      - projects